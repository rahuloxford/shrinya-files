#include <stdio.h>

int main() {

    int number = 25;
    // number++; // number = number + 1;
    // number++;
    // number--;

    // postfix (++, --)
    // printf("%d", number--);
    // printf("%d", number);

    // prefix (++, --)
    printf("%d", --number);
    printf("%d", number);

    return 0;
}