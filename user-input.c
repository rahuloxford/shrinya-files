#include <stdio.h>

int main() {

    // printf("What's your age? ");
    // int age;
    // scanf("%d", &age);

    // printf("user age is: %d", age);


    // take user marks as input?

    printf("enter your marks? ");
    float marks;
    scanf("%f", &marks);
    printf("person scored:- %.1f", marks);
    
    return 0;
}